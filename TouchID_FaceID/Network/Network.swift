import Foundation

// MARK: - Namespace
enum Network {
    /// Common
    enum Common {
        /// Requests
        enum Requests  {}

        /// Responses
        enum Responses {}
    }

    /// Core
    enum Core {
        /// Plugins
        enum Plugins {}
    }

    /// SampleProvider
    enum SampleProvider {}
}
